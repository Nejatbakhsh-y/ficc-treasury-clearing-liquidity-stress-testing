# Configurations

Store version-controlled YAML configuration for data sources, model assumptions, Cover 1 and Cover 2 definitions, stress scenarios, validation tolerances, and reporting. Every modeled result must identify the configuration version that produced it.
