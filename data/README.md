# Data

Only public, synthetic, or appropriately licensed data may be used. Generated raw, interim, processed, and synthetic datasets are ignored by Git. Commit data dictionaries, source registers, manifests, schemas, and small approved fixtures instead of downloaded datasets.
